# novo-test

New project. `backend/` has a minimal Node.js + TypeScript + Express +
SQLite POC backend (see `backend/package.json` for scripts). A React +
TypeScript/Vite frontend is still to be added.

## AI agent tooling

- **lavish** — rich HTML artifact / plan review (`npx -y lavish-axi`)
- **gh-axi** — GitHub CLI for agents (`npx -y gh-axi`)
- Managed as a **firstmate** project for crewed, parallel task execution

See [AGENTS.md](./AGENTS.md) for details.
